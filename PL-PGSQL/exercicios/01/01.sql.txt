CREATE OR REPLACE FUNCTION numeroDeAlunos() returns INT
AS $$
DECLARE VALOR INT;
BEGIN
	SELECT COUNT(*) FROM ALUNO, MATRICULA INTO VALOR WHERE ALUNO.CODALUNO = MATRICULA.CODALUNO;
RETURN VALOR;
END;
$$ LANGUAGE 'plpgsql';

SELECT numeroDeAlunos();